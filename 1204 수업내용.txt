for()블록에는 세미콜론이 들어가지 않는다
네 가지 꼭 알기 -> if 문, switch문, for문, while

length는 개수를 의미한다.

i++ ; 인덱스가 1씩 증가한다. 오름차순  i=i+1  i+=1  와 같다.
i--; 1씩 감소. 내림차순 i=i-1    i-=1   와 같다.

태그에 직접 function을 넣어주는 것 ; 인라인 function